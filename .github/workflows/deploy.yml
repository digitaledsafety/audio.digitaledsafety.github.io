name: Deploy to GitHub Pages

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  workflow_dispatch:
  
jobs:
  call-jekyll-deployment:
    uses: digitaledsafety/workflows/.github/workflows/reusable-jekyll-playwright-deploy.yml@android-packaging-support-16705636402519762902
    with:
      ruby-version: '3.2'
      node-version: '20'
